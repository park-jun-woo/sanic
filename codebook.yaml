required:
  feature:
    core: "core framework — app lifecycle, config, constants, signals"
    routing: "URL routing, route resolution, views"
    request: "HTTP request parsing, cookies, form, parameters"
    response: "HTTP response types, convenience builders"
    blueprint: "blueprint and blueprint group composition"
    middleware: "request/response middleware pipeline"
    server: "server startup, protocols, event loop, socket"
    http: "HTTP/1.1 and HTTP/3 protocol handling, TLS"
    websocket: "WebSocket protocol, connection, frame handling"
    worker: "multi-process worker management, reloader, inspector"
    cli: "CLI interface, arguments, console, daemon"
    error: "error pages, exception classes, error handling"
    logging: "logging setup, formatters, filters, colors"
    asgi: "ASGI interface and lifespan"
    mixin: "app mixin classes — routes, listeners, startup, static"
    model: "data models — handler types, protocol types, futures"
    page: "built-in HTML page rendering"
    touchup: "touchup optimization schemes"
    base: "base metaclass and root class"
    compat: "compatibility utilities"
    util: "general utilities and helpers"
  type:
    handler: "request/event handler"
    model: "data structure / class"
    protocol: "network protocol implementation"
    formatter: "output/log formatter"
    mixin: "mixin class providing partial behavior"
    builder: "factory / builder function"
    parser: "parsing logic"
    runner: "server/process runner"
    manager: "process/resource manager"
    resolver: "route/type resolver"
    config: "configuration container"
    exception: "exception class"
    util: "general utility function"
    middleware: "middleware function"
    constant: "constant definitions"
    page: "HTML page renderer"
    filter: "log/data filter"
    router: "routing and signal routing"

optional:
  pattern:
    error-collection: "collect errors for batch reporting"
  level:
    error: ""
    warning: ""
